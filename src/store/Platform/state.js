const initializeState = {
  note: {
    dialog: {
      createNewNote: {
        isOpen: false,
        default: {
          title: "New Note"
        }
      }
    }
  }
};

export default initializeState;
